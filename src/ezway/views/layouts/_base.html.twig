<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>{{pageName}}</title>
    {% for style in styles %}
        <link rel="stylesheet" href="{{style|e}}">
    {% endfor %}
</head>
    <body>
        {% block content %}{% endblock %}

        {% for script in scripts %}
            <script src="{{script|e}}" type="text/javascript" defer="defer"></script>
        {% endfor %}
    </body>
</html
